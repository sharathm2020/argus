# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.115.4
uvicorn[standard]==0.32.0

# ── LangChain / LLM ───────────────────────────────────────────────────────────
langchain==0.3.7
langchain-openai==0.2.8
langchain-core==0.3.18

# ── Data / Finance ────────────────────────────────────────────────────────────
#  — stock fundamentals now fetched via Financial Modeling Prep API
requests==2.32.3
yfinance==1.2.0

# ── HTTP client (EDGAR REST API + async requests) ─────────────────────────────
# sec-edgar-downloader removed — EDGAR data now fetched via REST API using httpx
httpx==0.27.2

# ── Validation & config ───────────────────────────────────────────────────────
pydantic==2.9.2
python-dotenv==1.0.1
python-multipart==0.0.9

# ── ML / Sentiment model ──────────────────────────────────────────────────────
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.4.0+cpu
transformers==4.40.0
accelerate==0.29.0
huggingface_hub==0.23.0

# ── Database ──────────────────────────────────────────────────────────────────
supabase

# ── Note: uuid and asyncio are Python stdlib — no install required ─────────────
